import type React from "react";

const SearchForm: React.FC = () => {
	// This component is currently deprecated in favor of direct search implementation
	// Keeping basic structure for potential future use
	return (
		<div className="p-4">
			<p className="text-text-muted">
				Search functionality has been moved to SearchPage component.
			</p>
		</div>
	);
};

export default SearchForm;